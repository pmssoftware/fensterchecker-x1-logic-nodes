# fensterchecker-x1-logic-nodes
Displays open windows and generates compact KNX display texts.


To Do:
-Scalability:
 Create an input box inside GPA to scale from 1–64 windows without needing to hide them inside the Logic Editor.
